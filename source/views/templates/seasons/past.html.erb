---
title: "64Bites - Season"
---
<% if defined?(season) %>
  <section class="season">
    <h2><%= season.formatted_title %></h2>
  </section>
  <p><%= season.price.formatted %></p>
  <%= image_tag(season.poster_path, class: "thumbnail") %>
<% end %>
